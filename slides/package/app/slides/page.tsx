import { Deck } from "../../optimizely-slides";

// Interactive Optimizely product deck (Feature + Web Experimentation).
// Mounted at /slides — keep this path in sync with slidesConfig.deckPath.
export default function SlidesPage() {
  return <Deck />;
}
